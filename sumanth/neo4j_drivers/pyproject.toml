[project]
name = "neo4j-student-placement"
version = "0.1.0"
description = "Load student placement data into Neo4j database"
requires-python = ">=3.8"
dependencies = [
    "neo4j>=5.0.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = []
